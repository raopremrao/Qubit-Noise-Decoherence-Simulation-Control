import numpy as np

# Simulation parameters
TIME_STEPS = 200
TOTAL_TIME = 5

# Noise parameters
gamma_amplitude = 0.2
gamma_phase = 0.15
gamma_depolarizing = 0.1

# Initial State
psi0 = np.array([1, 0])
